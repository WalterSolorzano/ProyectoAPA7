appId: com.wordapa7.app
productName: WordAPA7
disableSanityCheckAsar: true
directories:
  output: dist-electron-builder
  buildResources: build
files:
  - dist/**/*
  - dist-electron/**/*
extraResources:
  # ── Python embebido (reemplaza PyInstaller) ──────────────────────────
  # El runtime de Python oficial viaja en resources/python-runtime/.
  # Usa python.exe firmado por Python Software Foundation en lugar del
  # exe de PyInstaller que Windows Defender flaggea como malware.
  # NO se requieren exclusiones en el antivirus del usuario final.
  - from: dist-python/python-runtime/
    to: python-runtime/
    filter:
      - "**/*"
  - from: word-addin/dist/
    to: addin/
    filter:
      - "**/*"
  - from: word-addin/manifest.xml
    to: addin/manifest.xml
compression: normal
win:
  target: nsis
  icon: build/icon.ico
  signtoolOptions: null
nsis:
  # ── Asistido: bienvenida con info de app + complemento ────────────────
  # oneClick: false muestra la página de Bienvenida (build/installer.nsh:
  # customWelcomePage) que explica los DOS componentes y los pasos automáticos,
  # más la página final con instrucciones para Word.
  # allowToChangeInstallationDirectory: false oculta la elección de carpeta
  # (instala igual en %LOCALAPPDATA%\Programs\WordAPA7\ sin UAC).
  oneClick: false
  allowToChangeInstallationDirectory: false
  createDesktopShortcut: true
  createStartMenuShortcut: true
  perMachine: false
  runAfterFinish: true
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico
  installerHeader: build/installerHeader.bmp
  installerSidebar: build/installerSidebar.bmp
  uninstallerSidebar: build/uninstallerSidebar.bmp
  include: build/installer.nsh
publish:
  provider: github
  owner: WalterSolorzano
  repo: ProyectoAPA7
  releaseType: release
